spring:
  datasource:
    url: jdbc:h2:mem:GameDevForumDB
    username: admin
    password: admin
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  liquibase:
    change-log: db/changelog-master.yml
  security:
    user:
      password: admin
      name: admin